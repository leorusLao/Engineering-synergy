<?php

namespace App\Models;
use Eloquent;

class CompanyIndustry extends Eloquent
{
	 
	/**
	 * The attributes that are mass assignable.
	 *
	 * @var array
	 */
	protected $primaryKey = 'industry_id';
	protected $table = 'company_industry';

	protected $fillable = array('industry_code','name','name_en','type', 'host_domain', 'is_active');
	protected $guarded = array('industry_id');
	public $timestamps = true;

 
}