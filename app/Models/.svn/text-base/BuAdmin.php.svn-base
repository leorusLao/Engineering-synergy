<?php

namespace App\Models;
use \Exception;
use Eloquent;
use DB;

class BuAdmin extends Eloquent {
    protected $primaryKey = 'id';
    protected $table = 'buadmin';
    protected $fillable = array (
            'uid', 'bu_id', 'bu_name', 'bu_site', 'role_id', 'is_approved', 'is_active'
    );
    protected $guarded = array (
            'id'
    );

    public $timestamps = true;
 

}

?>

